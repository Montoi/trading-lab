# Local development uses the SAME PostgreSQL database as production over SSH.
# Copy the real credentials from the server; do not generate a local database.
POSTGRES_PASSWORD=use-the-existing-contabo-database-password
DB_SSH_TARGET=root@62.171.152.158
DB_TUNNEL_PORT=15432
DB_REMOTE_PORT=15432
DEV_PORT=3000
# Docker Compose is for the server deployment only:
APP_PORT=3002
APP_BIND_ADDRESS=127.0.0.1

