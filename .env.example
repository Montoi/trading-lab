# Run npm run setup:env to create .env with a unique random password.
POSTGRES_PASSWORD=replace-with-generated-secret
APP_PORT=3000
# For development outside Docker use the standard PGHOST, PGPORT, PGUSER,
# PGDATABASE and PGPASSWORD variables, or DATABASE_URL.
